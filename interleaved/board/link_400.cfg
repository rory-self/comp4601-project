save-temps=1
temp_dir=link_tmp
report_dir=link_tmp/reports
log_dir=link_tmp/logs

[connectivity]
nk=arith_kernel:1:arith_kernel_1

# 400 MHz clock (id 2) for the g2 "max" attempt. May not close post-route.
[clock]
id=2:arith_kernel_1
